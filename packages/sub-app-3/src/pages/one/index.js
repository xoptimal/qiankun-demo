export default function One(props) {

    console.log("props",props)

    return (
        <div style={{background: '#f0f0f0', minHeight: 300, padding: 24}}>
            加载成功, 当前是sub-app-3/one 路由页面内容
        </div>
    )
}
